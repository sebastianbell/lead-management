export type LeadFormData = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  profile: string;
  visas: string[];
  countryOfCitizenship: string;
  resume: string;
  openInput: string;
  status?: string;
};
